import PageContent from "@/app/(protected)/transactions/_components/page-content";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Transactions",
};

const Transactions = () => {
  return <PageContent />;
};

export default Transactions;
